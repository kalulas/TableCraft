using System;
using System.Collections.Generic;
using System.Text;
using ConfigCodeGenLib.ConfigReader;

namespace ConfigCodeGenLib.CodeGenerate
{
    internal static class Generator
    {
        public static bool Process(ConfigInfo configInfo, string usage)
        {
            // TODO 1. is template compiled yet?
            // 2. if not: get template path, compile template
            // 3. generate code
            return true;
        }
    }
}
